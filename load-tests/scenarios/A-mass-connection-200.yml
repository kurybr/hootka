# Cenário A4: 200 participantes
config:
  target: "http://localhost:3000"
  phases:
    - name: "200 participants"
      duration: 60
      arrivalRate: 10
  engines:
    socketio-v3: {}
  processor: "../functions.js"
  variables:
    roomCode: "LOAD01"

scenarios:
  - name: "Join"
    engine: socketio-v3
    flow:
      - function: "setRoomCodeFromEnv"
      - function: "generateUniqueName"
      - emit:
          - "room:join"
          - code: "{{ roomCode }}"
          - name: "{{ participantName }}"
        response:
          on: "room:joined"
      - think: 1
